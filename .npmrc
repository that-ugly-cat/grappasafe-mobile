# expo-router 6 pulls a newer react-dom (web support) than the pinned react
# used by the native app, which trips npm's strict peer check on install.
# The native build never uses react-dom, so relax the peer resolution.
legacy-peer-deps=true
